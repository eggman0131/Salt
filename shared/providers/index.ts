/**
 * Shared Providers
 * 
 * AI INSTRUCTION:
 * Export all context providers from this barrel.
 * Wrap your app with providers in App.tsx or index.tsx
 * 
 * @example
 * import { ThemeProvider } from '@/shared/providers';
 * 
 * <ThemeProvider>
 *   <App />
 * </ThemeProvider>
 */

export { ThemeProvider, useTheme } from './ThemeProvider';
